[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter api build && pnpm --filter api db:generate"

[deploy]
startCommand = "node apps/api/dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "api"
